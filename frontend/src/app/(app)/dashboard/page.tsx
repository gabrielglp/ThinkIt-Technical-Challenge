import { DashboardClient } from "./_components/dashboard-client";

export const metadata = {
  title: "Pedidos | Orders Dashboard",
};

export default function OrdersPage() {
  return <DashboardClient />;
}
